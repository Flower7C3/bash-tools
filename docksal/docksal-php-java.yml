version: "2.1"
services:

### uncomment for auto import sql from .docksal/dist/db/ directory
#    db:
#        volumes:
#            - ${PROJECT_ROOT}/.docksal/dist/db/:/docker-entrypoint-initdb.d:ro

### uncomment for cli image with java binary
#    cli:
#        image: ${COMPOSE_PROJECT_NAME_SAFE}_cli
#        build: services/cli-php-java
